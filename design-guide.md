# ezREMS 로그인 UI 디자인 가이드

버전 1.0 · 2026-09-15

## 1. 목적

로그인, 2차 인증, 콘텐츠 관리자 화면에서 동일한 브랜드 경험과 접근성을 유지하기 위한 기준입니다.

## 2. 디자인 원칙

- 명확성: 한 화면의 핵심 행동은 하나의 700 굵기 버튼으로 강조합니다.
- 신뢰감: ezREMS Blue와 Deep Navy를 주요 색으로 사용합니다.
- 절제: 장식 목적의 800 이상 굵기와 불필요한 색상 사용을 피합니다.
- 접근성: 색상만으로 상태를 표현하지 않고 문구, 굵기, 아이콘을 함께 사용합니다.
- 호환성: 모바일 850px 이하에서도 핵심 기능과 오류 메시지가 누락되지 않아야 합니다.

## 3. 색상 토큰

| 토큰 | 값 | 용도 |
|---|---|---|
| Blue | #0C4DA2 | 핵심 버튼, 링크, 선택 상태 |
| Deep | #082F68 | 그라데이션, 강조 배경 |
| Navy | #071B34 | 어두운 텍스트와 보안 배경 |
| Red | #EF292D | 오류, 긴급 상태 |
| Ink | #172033 | 제목과 본문 주요 텍스트 |
| Muted | #68758A | 설명과 보조 정보 |
| Line | #DFE5EC | 테두리와 구분선 |
| Soft | #F3F6FA | 보조 배경 |
| Success | #087D61 | 성공 상태 |
| White | #FFFFFF | 카드와 반전 텍스트 |

## 4. 타이포그래피

기본 서체는 `Paperlogy, Pretendard, "Noto Sans KR", system-ui, sans-serif`입니다.

| 용도 | 굵기 |
|---|---:|
| 페이지 제목, 중요 영역 제목 | 700 |
| 핵심 실행 버튼, 선택된 탭 | 700 |
| 상태 배지, 보조 버튼 | 600 |
| 입력 라벨, 중간 강조 | 500 |
| 설명, 출처, 본문, 보조 정보 | 400 |

800 이상은 신규 UI에서 사용하지 않습니다. 같은 행의 데이터명과 수치를 모두 굵게 표시하지 않습니다.

## 5. 간격과 형태

- 기본 간격 단위: 4px
- 입력·버튼 높이: 40~52px
- 입력·버튼 모서리: 8~11px
- 카드 모서리: 12~18px
- 주요 화면 그림자: `0 24px 70px rgba(8,38,77,.16)`
- 터치 대상 최소 크기: 40px

## 6. 컴포넌트

### 버튼

- Primary: Blue 배경, 흰색 텍스트, 700
- Secondary: 흰색 또는 Soft 배경, Line 테두리, 600
- Danger: Red 계열 텍스트와 명확한 확인 문구
- Disabled: 회색 배경, 그림자 제거, 클릭 차단

### 입력

라벨은 입력 위에 배치하고 오류 시 테두리, 오류 문구, 포커스 이동을 함께 제공합니다. placeholder는 라벨을 대신하지 않습니다.

### 배지

상태 배지는 600 굵기를 사용하고 사용/숨김, SaaS/On-Premise처럼 상태 문구를 함께 표시합니다.

### 알림

긴급공지는 제목, 본문, 기간과 닫기 수단을 제공합니다. 로그인 오류는 입력 영역 가까이에 고정 공간을 확보해 레이아웃 이동을 최소화합니다.

## 7. 마케팅 패널

- 패널 전체형: 이미지가 왼쪽 마케팅 패널 전체에 표시됩니다.
- 카드형: 기본 패널 위 슬라이드 콘텐츠 카드에 이미지를 표시합니다.
- 이미지 없음: 기존 Blue–Deep–Navy 그라데이션을 사용합니다.
- 텍스트 보호 오버레이: 0.30~0.72
- 이미지 형식: JPG, PNG, WebP, 최대 5MB
- 이미지 경로는 `./content/images/backgrounds/` 상대 경로를 사용합니다.

관리자 테마는 `themes[]`에 저장하고 `activeThemeId`로 기본 테마를 지정합니다. 로그인 팔레트에서 사용자가 테마를 선택하면 브라우저에 저장하며 관리자 기본값으로 복원할 수 있습니다.

## 8. 2차 인증

SMS/이메일 선택, 6자리 입력, 남은 시간, 확인, 재발송을 제공합니다. 숫자 자동 이동, 전체 붙여넣기, 만료 안내와 오류 메시지를 지원합니다.

## 9. 반응형

- 851px 이상: 좌우 분할 레이아웃
- 850px 이하: 세로 흐름 및 간격 축소
- 420px 이하: 인증 입력 간격과 버튼 폭 최적화
- 화면 구성은 카드형과 전체 화면형을 지원합니다.

## 10. 접근성 및 검수

- 아이콘 전용 버튼에 `aria-label` 제공
- 장식 아이콘은 `aria-hidden="true"`
- 키보드 포커스 표시 유지
- 오류 메시지는 `role="alert"` 또는 연결된 설명 사용
- 모션 감소 설정 존중
- 텍스트 대비와 200% 확대 상태 확인
- 이미지 실패 시 대체 텍스트 또는 기본 배경 제공
